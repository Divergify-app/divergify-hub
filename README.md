# Divergify Hub

This repo is for the new Divergify app.

Important:
- The website repo inside `active-site/` is NOT committed here.
- This repo will contain the app source in `apps/divergify-hub-app/`.

Status:
- Bootstrap scaffold committed so GitHub + Netlify can build from real commits.
