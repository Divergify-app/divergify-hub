[build]
  command = "cd apps/divergify-hub-app && npm ci && npm run build"
  publish = "apps/divergify-hub-app/dist"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "development"
  NPM_FLAGS = "--include=dev"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
